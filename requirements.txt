# MedTex - Medical Text Extraction System
# 
# QUICK SETUP:
# 1. Install Python deps: pip install -r requirements.txt
# 2. Install Med7 model: pip install --no-deps en_core_med7_lg-0.0.1-py3-none-any.whl
# 3. Install Java 17: brew install openjdk@17 (see Data Processing section below)
# 4. Install frontend: cd frontend && npm install
# 5. Run backend: uvicorn backend.app.main:app --reload --port 8080
# 6. Run frontend: cd frontend && npm run dev
#
# --- Web Framework ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# --- Data Validation ---
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- NLP / ML ---
spacy==3.7.0
scispacy>=0.5.1
# Install Med7 model: pip install --no-deps en_core_med7_lg-0.0.1-py3-none-any.whl
# Note: Model trained with spaCy 3.4.2 but works with 3.7.0 (compatibility warning expected)
# Then install SciSpacy BioNLP model: pip install --no-deps https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.1/en_ner_bionlp13cg_md-0.5.1.tar.gz

# --- Data Processing ---
# NOTE: PySpark 3.5+ requires Java 17+ (not Java 11)
# Setup steps:
#   brew install openjdk@17
#   sudo ln -sfn /opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-17.jdk
#   echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17"' >> ~/.zshrc
#   echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
#   source ~/.zshrc
pandas>=2.1.0
pyspark>=3.5.0
pyarrow>=15.0.0
numpy>=1.24.0

# --- Document Parsing ---
pymupdf>=1.23.0
python-docx>=0.8.11
pillow>=10.0.0  # Image processing for OCR
pytesseract>=0.3.10  # OCR for images
# Install Tesseract: brew install tesseract (macOS) or apt-get install tesseract-ocr (Linux)

# --- Utilities ---
httpx>=0.25.0  # For HTTP requests and testing

# --- Task Queue (for batch jobs) ---
celery>=5.3.0
redis>=5.0.0

# --- Frontend (Node.js) ---
# Requires: Node.js 18+ and npm
# Install dependencies: cd frontend && npm install
# Run dev server: npm run dev
# Note: If npm install hangs with SSL errors, run: npm config set strict-ssl false

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.21.0