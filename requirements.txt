# MedTex - Medical Text Extraction System
# Compatible with: Python 3.12, PostgreSQL 16+
# Note: Python 3.14+ not supported (spacy incompatibility)

# --- Web Framework ---
fastapi>=0.128.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# --- Database ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# --- Authentication ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Background Tasks ---
celery>=5.3.0
redis>=4.5.0

# --- Monitoring ---
prometheus_client>=0.25.0

# --- Data Validation ---
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- NLP / ML ---
numpy==1.26.4
spacy==3.7.5
scispacy==0.6.2
transformers>=4.30.0
torch>=2.0.0
pyspark>=3.5.0

# --- OCR / File Processing ---
pytesseract>=0.3.10
pymupdf>=1.23.0
pillow>=10.0.0

# --- Fuzzy Matching (Medication Name Normalization) ---
rapidfuzz>=3.0.0

# --- VLM OCR (Groq API for Llama-4-Scout, Qwen3-VL) ---
groq>=0.9.0

# --- Utilities ---
httpx>=0.25.0

# --- Spacy Models (install separately after requirements.txt) ---
# python -m spacy download en_core_web_sm
#
# Med7 - Download manually due to wheel filename issue:
# curl -L https://huggingface.co/kormilitzin/en_core_med7_lg/resolve/main/en_core_med7_lg-any-py3-none-any.whl -o en_core_med7_lg-0.0.1-py3-none-any.whl
# pip install en_core_med7_lg-0.0.1-py3-none-any.whl --no-deps
# rm en_core_med7_lg-0.0.1-py3-none-any.whl
#
# SciSpacy models (v0.5.3 for spacy 3.6.1 compatibility):
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.3/en_ner_bionlp13cg_md-0.5.3.tar.gz
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.3/en_ner_bc5cdr_md-0.5.3.tar.gz
#
# Note: BC5CDR model installation may downgrade spacy to 3.6.1 for compatibility